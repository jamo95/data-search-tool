README

    Scripts for running and testing data search program.
    Run python3 searcher.py in the command line to run.
    Run python3 test_searcher.py in the command line to run tests.

INSTALL

    Requires python3 to run. Install by running brew install python3 in unix command line.

ASSUMPTIONS

    If there is a search option or search term that does not exist, the program will throw an "Input Error" exception. This does not include missing values which instead outputs "No results found".
    Search can handle both json and python syntax for boolean i.e. true & True, false & False

AUTHOR

    Jamison Tsai
